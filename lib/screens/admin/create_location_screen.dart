import 'package:flutter/material.dart';

class CreateLocationScreen extends StatelessWidget {
  const CreateLocationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Alta de Locaciones')),
      body: const Center(child: Text('Formulario Alta de Locación (Pendiente)')),
    );
  }
}
